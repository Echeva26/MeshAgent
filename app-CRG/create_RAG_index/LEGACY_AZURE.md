# Legacy Azure Indexing

The notebooks in this directory are retained as historical references for the previous Azure AI Search setup.

Local RAG indexes are now built from the repository root with:

```bash
python scripts/build_qdrant_indexes.py --app app-CRG
```
